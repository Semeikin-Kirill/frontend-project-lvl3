name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    steps:
      - uses: action/checkout@v2
      - uses: actions/setup-node@v1
      - run: make install
      - run: make lint
